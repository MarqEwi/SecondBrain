# Kopieren nach .env und Token eintragen. .env ist gitignored.
# Token anlegen: GitHub, Settings, Developer settings, Personal access tokens,
# Fine-grained token, nur Repo "SecondBrain", Permission "Contents: Read-only".
REPO_URL=https://MarqEwi:<TOKEN>@github.com/MarqEwi/SecondBrain.git
INTERVAL_SECONDS=3600
